plugins {
  id 'jetty'
}

apply plugin: 'war'

dependencies {
    compile project(':dynomitemanager')
}

jettyRun.contextPath = ''
jettyRunWar.contextPath = ''


// apply plugin: 'application'

// mainClassName = "com.netflix.dynomitemanager.Runner"
